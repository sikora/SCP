= simple_form_for(@perfil) do |f|
  = f.error_notification

  .form-inputs
    = f.input :nmperfil, label: 'Nome'
    = f.input :descperfil, label: 'Descrição'

  .form-actions
    = f.button :submit, value: 'Salvar'
