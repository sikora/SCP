= simple_form_for(@perfil) do |f|
  = f.error_notification

  .form-inputs
    = f.input :nm_perfil, label: 'Nome'
    = f.input :descricao, label: 'Descrição'

  .form-actions
    = f.button :submit, value: 'Salvar'
