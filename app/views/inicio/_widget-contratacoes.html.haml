%div.widget
  %div.widget-header
    %i.icon-warning-sign
    Últimas Contratações
    %div.pull-right
      %a ver todos
      %i.icon-arrow-down
  %div.widget-content
    - if @contratacoes.present?
      - @contratacoes.each do |contratacao|
        %blockquote
          %p
            %a
              = link_to  contratacao.pessoa.nm_pessoa , contratacao
          %small Data Contratação: #{contratacao.created_at.strftime('%d/%m/%Y')}
          %small Local: #{contratacao.vaga.lotacao.breadcrumb}
    - else
      %p.text-info Sem dados cadastrados
