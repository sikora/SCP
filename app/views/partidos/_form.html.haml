= form_for @partido do |f|
  -if @partido.errors.any?
    #error_explanation
      %h2= "#{pluralize(@partido.errors.count, "error")} prohibited this partido from being saved:"
      %ul
        - @partido.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label "Sigla"
    = f.text_field :nmpartido
  .field
    = f.label "nome"
    = f.text_field :nmcompleto
  .actions
    = f.submit 'Salvar', :class => "btn btn-success"
